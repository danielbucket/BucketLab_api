{
  "name": "bucketlab_app_server",
  "version": "0.4.20",
  "description": "Server running the BucketLab Empire public API",
  "main": "server.js",
  "scripts": {
    "test": "NODE_ENV=test jest --runInBand --verbose",
    "test:watch": "NODE_ENV=test jest --watch --verbose",
    "test:coverage": "NODE_ENV=test jest --runInBand --coverage",
    "start:dev": "NODE_ENV=development nodemon ./server.js",
    "start:prod": "NODE_ENV=production node ./server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.0.5",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.5.0",
    "http-proxy-middleware": "^3.0.5",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.10.1",
    "morgan": "^1.10.0",
    "validator": "^13.15.0"
  }
}
