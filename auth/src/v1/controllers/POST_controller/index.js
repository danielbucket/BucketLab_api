const { login_account } = require('./login_account');
const { logout_account } = require('./logout_account');
const { new_account } = require('./new_account');

module.exports = {
  login_account,
  logout_account,
  new_account
};